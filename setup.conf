LATEXMK_ARGS=(
    -pdflua 
    -file-line-error 
    -halt-on-error 
    -interaction=nonstopmode
)

BUILD_TYPES=(
    academic
    professional
)

COVER_LETTERS=( 
    coverletters/*.tex
)

OUTPUT_DIR=${OUTPUT_DIR:-"./build"}